# GoodGameRuler's Dotfiles

## Features

## Installation Instructions

### Dependencies

``` sudo pacman install pyenv nvm go ```

- Install rust from rustup source

``` sudo pacman install rofi alacritty polybar neovim zsh ```

Pulling the repo
``` git clone --recursive```
